#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# push 前全量校验：error/warning 均失败，禁止推送（不写回文件）
echo "Running Biome check before push..."
pnpm exec biome check --no-errors-on-unmatched --error-on-warnings
