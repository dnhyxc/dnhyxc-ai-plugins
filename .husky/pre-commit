#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 在提交前检查暂存区：error/warning 均失败，禁止提交（不写回文件）
echo "Running Biome check on staged files..."
pnpm exec biome check --staged --no-errors-on-unmatched --error-on-warnings
